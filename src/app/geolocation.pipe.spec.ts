import { GeolocationPipe } from './geolocation.pipe';

describe('GeolocationPipe', () => {
  it('create an instance', () => {
    const pipe = new GeolocationPipe();
    expect(pipe).toBeTruthy();
  });
});
